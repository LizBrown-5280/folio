import { createStore } from 'vuex';
import * as foodMenu from './modules/foodMenu';

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    foodMenu,
  },
});
